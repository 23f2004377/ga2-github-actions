name: cache-test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: prime-cache-71c854a
        id: prime-cache-71c854a
        uses: actions/cache@v4
        with:
          key: cache-71c854a
          path: ~/.cache/pip

      - name: echo-cache-status
        run: echo "Cache hit? ${{ steps.prime-cache-71c854a.outputs.cache-hit }}"

      - name: Install dependencies
        run: pip install -r requirements.txt
